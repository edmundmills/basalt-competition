# model attributes
cnn_layers: 12  # max is 17
linear_layer_size: 512
n_observation_frames: 1  # includes current frame

# lstm
lstm: true
lstm_layers: 1
lstm_hidden_size: 512
lstm_sequence_length: 10
