defaults:
  - method: iqlearn
  - env: cave
  - pretraining: curiosity
  - _self_

exp_name: ''
project_name: ${env.name}
device: ???  # to be specified later
pretrain: ???
env: ???
method: ???
wandb: ???

checkpoint_frequency: 500

discount_factor: 0.99
n_observation_frames: 3
frame_selection_noise: 0

batch_size: 128
starting_steps: 2000
suppress_snowball_steps: 500
training_steps: 10000
learning_rate: 3e-4
alpha: 1e-2


algorithm: 'online_imitation'  # curiosity, supervised_learning, online_imitation, sac
loss_function: 'iqlearn'  # iqlearn, sqil

hydra_base_dir: ""

seed: 0
